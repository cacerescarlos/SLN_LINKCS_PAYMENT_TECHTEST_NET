Usuario contraseña de BD: sa , Pass@word123

1. Crear Imagen sql 2022 y Contenedor con compose basico docker-compose.yml:
- docker-compose up -d
- docker ps
- docker logs sqlserver_payment



================= 2. Ejecutar script para crear BD en contenedor SQL (Nota ejecutar SI O SI desde PowerShell en windown) ===========

- Copiar el archivo al contenedor:
docker cp LINKCS.PAYMENT.TECHTEST.INFRASTRUCTURE/Scripts/InitDatabase.sql sqlserver_payment:/tmp/InitDatabase.sql

- Luego ejecutar:
docker exec -it sqlserver_payment /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Pass@word123" -i /tmp/InitDatabase.sql -C

- Verificar
docker exec sqlserver_payment /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Pass@word123" -d PaymentDb -Q "SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'Payments';" -C

- Ver la estrucutra de la tabla
docker exec sqlserver_payment /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "Pass@word123" -d PaymentDb -Q "SELECT COLUMN_NAME, DATA_TYPE, CHARACTER_MAXIMUM_LENGTH FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'Payments';" -C

